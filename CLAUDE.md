# 미션 CLAUDE.md — B2-1

## 미션 정보
- 번호: B2-1
- 이름: 나만의 용돈기입장 프로그램 만들기
- 필수/선택: 필수
- 배정 시간: 60h
- 단계: 2단계 AI 도구학습
- 산출물 유형: A (개인 코드 repo)
  - 단, 터미널 전용 CLI라 **배포 URL은 해당 없음**. README에는 실행 방법·저장 파일 위치/형식·주요 명령 예시·CSV 스키마·구현 기능 목록·자유 선택한 기준값을 대신 싣는다 (§2-10)

## 파일
`요구사항_`·`이해_`·`평가준비_`·`평가문항.md`는 **Drive 전용** — 미션 `.gitignore`가 제외하므로 GitHub에는 안 올라간다.

- 요구사항 md: `요구사항_B2-1.md` (원본 `AI_Tools_B1-B7_missions.md` 835–1123줄. ⚠️ 원본은 B2-1/B2-2 제목·본문 스왑 — 원본 제목 "B2-2", 본문이 정본)
- 이해 md: `이해_B2-1.md` (`/us`가 생성 — 3단계 개념학습 교재)
  - ❓ 답변: `/us`가 준 ❓에 답하면 이 파일 하단 `## ❓ 질문 목록`에 `→ 결정·왜`를 in-place 기록 (기준·형식: `/us` SKILL.md §4)
- 평가문항.md: 없음 — 추후 등록
- 개인 repo: https://github.com/soy7yos/B2-1 (public. `soy7yos` 소유 + `sysy2204` collaborator)
- 팀 repo: 해당 없음

## 선행 미션 산출물
- 없음
- 확보 여부: [x]

## 금지·제한 (미션 명세 원문 그대로)
- Python 3.10 이상 (§6)
- **표준 라이브러리만.** `pip install`이 필요한 외부 라이브러리 사용 금지 (§7)
- 저장 포맷은 **JSONL 또는 CSV 중 1개**만 선택 (§7)
- 저장 파일은 **3개 이상**으로 분리해 영구 저장 — transactions / categories / budgets (§7, §4-3)
- 옵션 표기는 리눅스 표준 `--`로 통일 (§4-1, §7)
- **스택트레이스 출력 금지** → 원인 + 해결 힌트로 출력 (§7, §4-13)
- 정상 종료 exit code 0, 오류 종료는 0이 아닌 값 (§7, §4-13)
- 데이터 모델은 `dataclass` 또는 그에 준하는 구조 + **클래스 최소 2개** (§4-2)
- **모듈 최소 3개**로 분리 (권장: CLI / 서비스 / 저장소 / 모델) (§4-14)
- list·search는 파일 전체 로드 금지 → **제너레이터 스트리밍** 필수 (§4-5, §4-7)
- 공통 관심사 **데코레이터 1개 이상** 구현 + 실제 적용 (§4-12)

## 시크릿
- 없음

## README 필수 항목
`/us`가 요구사항 전문에서 뽑아 채우고, `/ex`가 5기준 점검표 ⑤에서 대조한다. 명세가 지목한 항목이 빠지면 감점이 아니라 요구사항 미충족이다.

- [ ] (`/us`가 채움)

## 단계별 진행 상황
`/us`가 채우고 `/st N 완료`가 체크한다. 1 step = 1 커밋 = 1 로그 = 30~90분.

- [ ] (`/us`가 채움)

## 완료 단계 요약
-

## 에러 & 해결 기록
-

## 미션 특이사항
- 보너스(선택): 백업 기능 / 반복 내역 기능 / 출력 테이블 정렬 / 저장 원자성 강화(임시파일+rename)
- 원본 요구사항에 문서로 "명확히 고정"하라는 선택지 3곳: `update` 방식(옵션 vs 대화형, §4-1·§4-6) / 빈 카테고리 파일 초기 동작(안 A 자동생성 vs 안 B 차단, §4-3) / 저장 포맷(JSONL vs CSV, §7)
- 원본 마크다운 깨짐 3건은 `요구사항_B2-1.md` 상단 주석 참조 (제목 스왑 · §4 번호 리셋 · 홑대시→겹대시)
